#TCP传输基本原理
	1.发送方发送一个分组，然后等待接收方的ACK。
	2.接收方收到这个分组后，给发送方回应一个ACK。
	3.当发送方接到ACK时，再发送下一个分组。
	
##关于TCP传输可靠性问题
	1.如果ACK丢失怎么办
	2.发送方Send后，等待ACK多长时间算超时？
	3.分组接到后，如何判断里面有错

##关于TCP传输性能问题
	1.如何优化一个很小分组的传输。
	2.滑动窗口提高传输效率
	3.如果解决网络拥堵的问题